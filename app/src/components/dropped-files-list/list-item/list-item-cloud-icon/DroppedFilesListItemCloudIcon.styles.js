const styles = theme => ( {
    iconCloud: {
        fontSize: theme.sizing.sizeS,
        marginRight: theme.spacing.sizeM,
        color: theme.palette.grey[500]
    },

    buttonRetry: {
        marginRight: theme.spacing.sizeS,
    },

    iconCloudRetry: {
        fontSize: theme.sizing.sizeS,
        color: theme.palette.grey[500]
    }
} );

export default styles;
